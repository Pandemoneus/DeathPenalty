name: DeathPenality
main: com.pandemoneus.deathPenality.DeathPenality
version: '1.01'
author: 'Pandemoneus'

commands:
    deathpenality:
        description: NewChunkNotifier
        usage: |
               /<command>
    dp:
        description: NewChunkNotifier
        usage: |
               /<command>